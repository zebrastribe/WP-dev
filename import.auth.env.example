# HTTP Basic Auth for /import/ on staging/production (not used for local Docker).
# Copy to import.auth.env and set credentials. import.auth.env is gitignored.
IMPORT_BASIC_AUTH_USER=Terraforming_mars
IMPORT_BASIC_AUTH_PASSWORD=change-me
